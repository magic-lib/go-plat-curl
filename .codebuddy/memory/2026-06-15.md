# 2026-06-15

- 重写了 `HttpMiddlewareInterceptor`（`curl/middleware.go`）：修复了原有错误签名和实现，改为标准 HTTP 中间件模式（`func(next http.HandlerFunc) http.HandlerFunc`）。新增 `responseWriterWrapper` 用于捕获状态码和响应体。中间件流程：捕获请求体 → 包装 ResponseWriter → 调用 next handler → 构建 Response 对象（含 Request/Method/Url/Data/Header、Response body、StatusCode、CostTime）→ 回调 logCallback。
